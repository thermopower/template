## 출처
- 저장소: 1
- sprint: sprint-001 ~ sprint-003
- 날짜: 2026-04-08

## 발견한 문제점

**반복 패턴 — 타입 안전성 우회**
- Sprint 1, 2, 3에서 `as unknown as` 이중 캐스팅이 반복 재발. 이전 sprint에서 제거한 후 신규 파일 작성 시 동일 안티패턴이 다시 도입됨.
- `@ts-expect-error` 사유 없는 주석 패턴도 반복 확인.

**반복 패턴 — Next.js Server/Client 경계 위반**
- Sprint 1에서 RSC(Server Component)가 Client Component에 이벤트 핸들러를 prop으로 전달하여 fix_attempt 1회 소모.

**반복 패턴 — PDF/문서 렌더링 한글 폰트 누락**
- Sprint 3에서 PDF 렌더링 기능 구현 시 CJK 폰트 미등록으로 핵심 기능 무용지물. `Helvetica` 기본 폰트만 사용하여 구현 완료 선언.

**blocker 유형 — implemented 후 단계 이탈**
- Sprint 1에서 status: implemented 전환 후 evaluator 실행 전에 사용자 지시에 따라 추가 구현 진행. 하네스 단계 전환 규칙 위반 2회.

**반복 패턴 — 신규 파일 작성 시 기존 패턴 미확인**
- Sprint 2, 3 공통: 기존 코드에서 안티패턴을 제거했음에도 신규 파일 작성 시 동일 패턴 재도입. 사전 grep 스캔 부재.

## 적용 상태
2026-04-08 반영 완료

## 적용한 개선

### 1. implementer.md — `## 신규 파일 작성 전 기존 패턴 스캔 (필수)` 섹션 신규 추가
- 신규 파일 작성 전 유사 기능 기존 파일 grep 스캔 의무화
- `as unknown as`, `@ts-expect-error` 사유 없는 주석, 빈 catch 블록 안티패턴 명시 및 구현 완료 전 0건 확인 요건 추가
- Next.js Server/Client 함수 prop 전달 금지 및 Server Action/Client Component 분리 원칙 추가
- PDF/CJK 폰트 `Font.register()` 미등록 시 구현 완료 불인정 명시

### 2. evaluator.md — 타입 안전성 회귀 검사 단계(5번) 신규 추가
- `grep -rn "as unknown as" src/` 1건 이상 시 즉시 fail 판정
- 사유 주석 없는 `@ts-expect-error` 발견 시 fail 항목으로 처리

### 3. sprint-builder.md — `## 금지사항`에 implemented 이후 추가 구현 금지 항목 추가
- status: implemented 전환 후 사용자 요청에도 추가 구현 금지
- 위반 시 evaluator → reviewer → retrospective → 다음 sprint 흐름으로 안내하도록 명시

### 4. .ruler/AGENTS.md — `## Stack-Specific` TypeScript/Next.js 항목 강화
- `any` 절대 금지 → `unknown` + 타입 가드로 대체 명시
- `as unknown as` 이중 캐스팅 신규 도입 금지, 기존 코드 발견 시 즉시 제거 명시
- `@ts-expect-error` 허용 조건(라이브러리 타입 결함, 사유 주석 필수) 명시
- Next.js App Router Server Component → Client Component 함수 prop 전달 금지 및 대안 명시
